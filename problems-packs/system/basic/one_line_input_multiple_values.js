(function()
{
  const problem = {"title":"한줄로 여러 값 입력받기","content":"여러분은 앞으로 많은 문제에서 한줄로 여러개의 데이터를 입력받게 될 것입니다.\n만약 문제가 아래와 같다고 해봅니다.\n\n---\n\n한줄에 공백을 기준으로 3개의 값을 입력으로 줄것입니다.\n각각을 한줄씩 출력하세요.\n\n---\n\n입력은 `a b c`와 같을 겁니다.\n그러면 여러분은 아래와 같이 출력해야만 합니다.\n```\na\nb\nc\n```\n어떻게 할 수 있을까요?\n간단합니다. javascript의 split 문자열 메서드를 사용하세요.\n\n``` javascript\nconst splitedInputs = readline().split(' ');\nprint(splitedInputs[0]);\nprint(splitedInputs[1]);\nprint(splitedInputs[2]);\n```\n\n그러면 문제입니다.\n\n---\n\n공백을 기준으로 2개의 값이 입력됩니다.\n이 두개의 순서를 바꿔서 한줄 씩 출력하세요.","openTestCases":[{"input":"a b","output":"b\na"},{"input":"hello hi","output":"hi\nhello"},{"input":"what's up","output":"up\nwhat's"}],"randomTestCase":"const chars = [];\r\nconst ranges = [['a', 'z'], ['A', 'Z'], ['0', '9']];\r\nfor (const range of ranges)\r\n{\r\n    const start = range[0].charCodeAt(0);\r\n    const end = range[1].charCodeAt(0);\r\n    for (let i = start; i <= end; i++)\r\n    {\r\n        chars.push(String.fromCharCode(i));\r\n    }\r\n}\r\n\r\nfunction randomWord()\r\n{\r\n    const randomChars = [];\r\n    const length = Math.floor(Math.random() * 20) + 1;\r\n    for (let i = 0; i < length; i++)\r\n    {\r\n        const random = Math.floor(Math.random() * chars.length);\r\n        const char = chars[random];\r\n        randomChars.push(char);\r\n    }\r\n    \r\n    return randomChars.join('');\r\n}\r\n\r\nconst word1 = randomWord();\r\nconst word2 = randomWord();\r\n\r\nprintInput(`${word1} ${word2}`);\r\nprintOutput(word2);\r\nprintOutput(word1);","solution":"const splitedInputs = readline().split(' ');\r\nprint(splitedInputs[1]);\r\nprint(splitedInputs[0]);"}
  loadedProblem(problem);
})();